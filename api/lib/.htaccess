# These files are PHP includes, never web endpoints. Deny everything.
# (Belt and braces: the parent /api/.htaccess already 404s this path.)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
